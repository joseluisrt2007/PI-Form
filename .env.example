# Copia este archivo como .env.local SOLO si vas a probar la función
# localmente con `vercel dev`. En producción, esta variable se configura
# desde el panel de Vercel (Project Settings -> Environment Variables),
# NUNCA se sube al repositorio.

GEMINI_API_KEY=tu-clave-de-google-ai-studio-aqui
